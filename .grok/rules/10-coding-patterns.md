# Coding patterns

Before writing Server Actions, client components, forms, or Playwright tests, open and follow:

**`.claude/rules.md`**

Canonical once: wrappers (`withNatsConnection` / `withJetStream`), `ActionResponse` narrowing, form stack, Playwright seed key, do/don't table.
