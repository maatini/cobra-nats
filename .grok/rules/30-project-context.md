# Project context

Feature ↔ route map, KV/OS stream prefixes, color palette, localStorage key, and dev setup:

**`.claude/project.md`**

Remember server-boundary exceptions: `GET /api/monitor` (SSE), `POST /api/os/upload` (multipart).
